{
  "name": "root",
  "private": true,
  "scripts": {
    "start:api": "lerna run build --scope=@recruit/interface && lerna run start:dev --scope=@recruit/api"
  },
  "dependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.3.5"
  },
  "devDependencies": {}
}
